data:
  root: "data/MML_Data"

gallery:
  include_index: false

inference:
  batch_size: 64
  device: "cuda"

evaluation:
  thresholds_km: [1, 25, 200, 750, 2500]

training:
  num_epochs: 10
  batch_size: 32
  lr: 1.0e-4
  num_workers: 4
  checkpoint_path: "models/best_geoclip_baseline.pth"
