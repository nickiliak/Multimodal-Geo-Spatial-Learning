# GeoClip zero-shot baseline configuration

data:
  root: "data/MML_Data"

gallery:
  source: index      # "train" (17k) | "index" (100k, paper Sec 5.2) | "both" (118k)

inference:
  batch_size: 64
  device: "cuda"

evaluation:
  thresholds_km: [1, 25, 200, 750, 2500]
